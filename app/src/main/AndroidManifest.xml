<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Permisos para acceso a contenido multimedia, puede ser riesgoso pal usuario -->
    <!-- accesos a internet no requiere permiso, para descargar la img -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" /> <!-- ACCESS_COARSE_LOCATION:REGION //   ACCESS_FINE_LOCATION: Ubicacion exacta -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <!--
    //Si no quiere valer la notificacion

    -->


  <application
      android:name=".ui.utilities.Dispositivos_moviles_proyecto_gc_es1"
      android:allowBackup="true"
      android:dataExtractionRules="@xml/data_extraction_rules"
      android:fullBackupContent="@xml/backup_rules"
      android:icon="@mipmap/ic_launcher"
      android:label="@string/app_name"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:supportsRtl="true"
      android:theme="@style/Theme.Dispositivos_moviles_proyecto_gc_es1"
      android:usesCleartextTraffic="true"
      tools:targetApi="31">
      <receiver android:name=".ui.utilities.BroadcasterNotifications"
          android:enabled="true"/>

      <activity
          android:name=".ui.activities.CameraActivity"
          android:exported="false" />
      <activity
          android:name=".ui.activities.MainActivity"
          android:exported="false" />
      <activity
          android:name=".ui.activities.ResultActivity"
          android:exported="false" />
      <activity
          android:name=".ui.activities.ActivityWithBinding"
          android:exported="false" />
      <activity
          android:name=".ui.activities.DetailsMarvelItem"
          android:exported="false" />
      <activity
          android:name=".ui.activities.NotificationActivity"
          android:exported="true">
          <intent-filter>
              <action android:name="android.intent.action.MAIN" />

              <category android:name="android.intent.category.LAUNCHER" />
          </intent-filter>
      </activity>

      <meta-data
          android:name="preloaded_fonts"
          android:resource="@array/preloaded_fonts" />
  </application>

</manifest>